# GitHub package notes

Run `python run_figure6.py` after restoring the documented experimental inputs
under `inputs/current/`. Those raster inputs and all final PDF/AI/TIFF files are
excluded from GitHub. The code package retains the evidence-strength safeguards
and unresolved-input records from the local review unit.
